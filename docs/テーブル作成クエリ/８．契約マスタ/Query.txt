CREATE TABLE contract_mst (
  uniqueid UUID DEFAULT gen_random_uuid() NOT NULL,
  contract_type_id SERIAL PRIMARY KEY NOT NULL,
  contract_type_name VARCHAR(100) NOT NULL,
  created_at timestamp DEFAULT now()
);