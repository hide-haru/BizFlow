CREATE TABLE invoice_mst (
  uniqueid UUID DEFAULT gen_random_uuid() NOT NULL,
  invoice_status_id SERIAL PRIMARY KEY NOT NULL,
  invoice_status_name VARCHAR(100) NOT NULL,
  created_at timestamp DEFAULT now()
);